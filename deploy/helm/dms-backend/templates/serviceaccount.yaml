{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dms-backend.serviceAccountName" . }}
  labels:
    {{- include "dms-backend.labels" . | nindent 4 }}
{{- end }}
