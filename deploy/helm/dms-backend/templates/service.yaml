apiVersion: v1
kind: Service
metadata:
  name: {{ include "dms-backend.fullname" . }}
  labels:
    {{- include "dms-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "dms-backend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
